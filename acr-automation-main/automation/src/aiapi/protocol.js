/**
 * AI API constants for poker actions.
 *
 * @type {{FOLD: string, CALL: string, CHECK: string, BET: string, RAISE: string, ALLIN: string}}
 */
const ACTIONS = {
    FOLD: "folds",
    CALL: "calls",
    CHECK: "checks",
    BET: "bets",
    RAISE: "raises",
    ALLIN: "allins",
};

module.exports = ACTIONS;
